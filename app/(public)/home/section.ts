/**
 * Every home section sits on the same grid as the navbar (max-w-[1240px]) and
 * carries the same vertical rhythm. Sections used to mix 1280/1380 widths and
 * py-4 through py-10, which made the page read as separately built strips
 * rather than one page.
 */
export const HOME_SECTION =
  "mx-auto w-full max-w-[1240px] px-4 py-8 font-sans sm:px-6 sm:py-10";

/** For a full-bleed tinted band: the band owns the padding, this the grid. */
export const HOME_BAND_INNER = "mx-auto w-full max-w-[1240px] px-4 sm:px-6";
